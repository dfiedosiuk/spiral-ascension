import org.scalatest._
import flatspec._
import matchers._
import Spiral._


class SpiralSpec extends AnyFlatSpec with should.Matchers {

  behavior of "A Spiral.create()"

  it should "return a spiral that begins with 1 and starts from the top left," +
    " going towards the right, and ends with the square of that number." in {
//    create(3) = Array(Array("1", "2", "3"), Array("8", "9", "4"), Array("7", "6", "5"))

  }

}